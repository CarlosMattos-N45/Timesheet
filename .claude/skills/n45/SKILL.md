---
name: n45
description: 'Start here: detects context and routes to create, fix, continue, or status automatically'
disable-model-invocation: true
---

Run the command below via `Bash` and follow the `content` field of the returned JSON:

- **macOS / Linux:** `.n45/bin/n45 init claude`
- **Windows:** `".n45/bin/n45.exe" init claude`

If `status: "error"` → show `message` to the user.

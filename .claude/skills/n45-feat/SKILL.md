---
name: n45-feat
description: 'Want to build something new? Plan and implement a feature end-to-end'
---

Run the command below via `Bash` and follow the `content` field of the returned JSON:

- **macOS / Linux:** `.n45/bin/n45 init claude -c feat`
- **Windows:** `".n45/bin/n45.exe" init claude -c feat`

If `status: "error"` → show `message` to the user.
